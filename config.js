const appsDirectory = "./apps";

const routerConfig = {
    eventEmitter: null,
};

const serverConfig = {
    hostname: "127.0.0.1",
    port: 5000,
    eventEmitter: null,
};

module.exports = {
    serverConfig,
    routerConfig,
    appsDirectory,
};
